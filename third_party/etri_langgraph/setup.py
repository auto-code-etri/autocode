from setuptools import find_packages, setup

setup(
    name="etri_langgraph",
    version="0.0.1",
    description="",
    url="",
    install_requires=[],
    packages=find_packages(exclude=[]),
    keywords=[],
    python_requires=">=3.8",
    package_data={},
)
